# Handoff — UI-COMPONENT 카탈로그

최종 갱신: 2026-08-14 (신규 세션에서 `/handoff` 스킬 로딩 검증 완료)

## ✅ 완료 (증거)
- [x] `index.html` — CareerFoundry 32 UI Elements + 레이어/알림 6종(Alert Dialog, Confirm Dialog, Toast, Snackbar, Bottom Sheet, Popover) = 총 38개 컴포넌트 카탈로그 작성.
      근거: `node --check`로 인라인 `<script>` 문법 검증 통과("JS SYNTAX OK"). Playwright로 실제 클릭까지 확인 — 사이드바 첫 그룹이 "레이어 · 알림 (6)"이고 항목 6개(Alert Dialog/Confirm Dialog/Toast/Snackbar/Bottom Sheet/Popover) 순서대로 렌더링됨, 항목 클릭 시 `.detail__title`/`.detail__ko`/`.detail__desc` 채워짐, HTML 탭에서 `code.hljs`에 하이라이트 span 12개 생성, 복사 버튼 클릭 후 텍스트가 "복사됨 ✓"로 바뀜, iframe 내부에서 Toast 트리거 클릭 시 `.ts` 요소 1개 생성 확인.
- [x] 오버레이 컴포넌트(Modal, Alert Dialog, Confirm Dialog, Bottom Sheet, Bento Menu)에서 `hidden` 속성이 `display:flex/grid` CSS에 가려 항상 열린 상태로 보이던 버그 수정.
      근거: 수정 전 Playwright로 Bottom Sheet의 `.bs` 요소 computed style 확인 시 `{ hiddenAttr: true, display: 'flex' }`로 버그 재현. 각 컴포넌트 CSS에 `.xxx[hidden] { display: none; }` 규칙 추가 후 재검증 — Bottom Sheet 패널이 트리거 클릭 전엔 안 보이고 클릭 후에만 보임(`bsVisible: true` after click), 전체 재실행 결과 "콘솔/페이지 에러: 없음".
- [x] `CLAUDE.md` 작성 및 3회 갱신 — 저장소 개요, 실행 방법, `index.html`의 데이터 구조(`ELEMENTS`/`ELEMENTS_2`/`LAYER_ELEMENTS`/`DATA`), iframe `srcdoc` 격리 방식, hljs 하이라이트 방식, `hidden`+`display` 충돌 함정, `rules/anti-ai-slop.md` 시각 스타일 규칙, `rules/settings.local.json`의 Stop 훅(`extract-my-prompts.sh` 자동 실행) 문서화.
      근거: `CLAUDE.md` 파일 존재 및 위 섹션들이 실제로 기록되어 있음(직접 Read로 확인).
- [x] `.claude/skills/handoff/SKILL.md` 작성 — 세션 인계 시 `.handoff.md`에 ✅(증거)/❌(실패, 반복 방지)/⏳(다음 할 일) 체크리스트를 남기는 프로젝트 스킬.
      근거: 파일 생성됨. 단, 생성 당시 세션에서는 `/handoff`(Skill 도구)가 "Unknown skill: handoff"로 실패 — 그 세션의 `.handoff.md`는 SKILL.md 절차를 수동으로 따라간 dry-run 결과물이었음.
- [x] 새 세션에서 `/handoff` 스킬이 실제로 로드·실행되는지 검증 — 성공.
      근거: 새로 시작된 이번 세션에서 `Skill({skill:"handoff"})` 호출 시 "Unknown skill" 에러 없이 SKILL.md 본문(원칙/절차/템플릿 전체)이 정상 주입됨. 이전 세션이 남긴 "세션 도중 생성한 스킬은 새 세션을 열어야 인식된다"는 가설이 실측으로 확인됨.

## ❌ 시도했지만 실패 (반복 방지)
- [ ] 브라우저 검증에 `chromium-cli`를 먼저 시도 — 설치되어 있지 않아(`which chromium-cli` → not found) 실패. `npx playwright install chromium --with-deps` + `npx playwright`로 대체해 성공. 다음에 브라우저 검증이 필요하면 `chromium-cli` 유무를 먼저 확인하고, 없으면 바로 Playwright(npx) 경로로 가는 게 빠르다.
- [ ] `/handoff` 스킬을 방금 만든 세션 안에서 바로 호출 — 실패(`Unknown skill: handoff`). Claude Code는 세션 시작 시점에 `.claude/skills/`를 로드하므로, 세션 도중 새로 만든 스킬은 **새 세션을 열어야** 인식된다(위 ✅ 항목에서 새 세션 재현으로 재확인). 같은 세션 내에서 재시도해도 소용없다.

## ⏳ 다음 할 일
- [ ] 현재 저장소 전체(`index.html`, `CLAUDE.md`, `docs/`, `rules/`, `.claude/`, `extract-my-prompts.sh`, `.gitignore`)가 아직 `git add`/커밋되지 않은 상태(`git status` 기준 전부 Untracked, `git log`는 여전히 `7881c4f first commit` 하나뿐). 커밋 여부와 메시지는 사용자 확인 필요 — 임의로 커밋하지 말 것.
- [ ] `extract-my-prompts.sh`가 `rules/settings.local.json`의 Stop 훅으로 세션 종료 시 자동 실행되도록 설정되어 있으나, 실제로 훅이 정상 동작해 `Prompt.md`가 갱신되는지는 아직 확인되지 않음.
      근거: 현재 `Prompt.md`는 0바이트(최종 수정 08-14 15:29)로, 이번 세션 시작(그 이후 시각) 이후 갱신된 적이 없음. 이번 세션 종료 후 파일 크기/내용이 바뀌는지 확인 필요.

## 참고
- `index.html`의 컴포넌트 데이터(`html`/`css`/`js`)는 `iframe srcdoc`으로 완전히 격리되므로, 새 컴포넌트를 추가할 때 메인 페이지 스타일에 의존하면 안 되고, `hidden` 토글형 오버레이는 반드시 `.xxx[hidden]{display:none;}` 패턴을 같이 넣어야 한다(`CLAUDE.md` §`index.html` 구조 참고).
- `.gitignore`는 현재 빈 파일 — 아직 무엇을 제외할지 정해지지 않은 상태.
